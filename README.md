# kalenteryanv javascript assignment 1

Q1. What is innerHTML?
A1. innerHTML is a property of DOM elements that gets or sets the HTML content inside an element.

Q2. What is the DOM?
A2. Dom is a programming interface for web documents that represents the structure of an HTML or XML document as a tree of objects.